version: '3.8'

services:
  backend:
    build: . 
    container_name: ragent-backend
    restart: always
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - GroqApiKey=${GROQ_API_KEY}